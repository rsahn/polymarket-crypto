# Optional archive RPC configuration template. Contains no credential.
# The diagnostic reads this name from the process environment only.
# This file is documentation, not automatically loaded.
POLYGON_ARCHIVE_RPC_URL=
